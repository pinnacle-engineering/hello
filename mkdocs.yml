site_name: My Docs

theme:
  name: material

plugins:
- mkdocstrings:
    handlers:
      python:
        paths: [src]  # search packages in the src folder

nav:
  - Hello Docs: index.md
  - reference.md
